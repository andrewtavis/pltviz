# This file may be used to create an environment using:
# conda env create -f environment.yml
name: stdviz-dev
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - numpy
  - scipy
  - pandas
  - matplotlib
  - seaborn
  - colormat
